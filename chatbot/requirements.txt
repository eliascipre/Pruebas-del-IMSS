fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.0
httpx==0.27.0
pillow==10.2.0

# LangChain ecosystem
langchain==0.3.0
langchain-core==0.3.0
langchain-openai==0.2.0
langchain-community==0.3.0
langsmith==0.1.130

# Async support
aiofiles==23.2.1

# Pydantic for FastAPI
pydantic==2.10.2

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Performance optimizations
aiosqlite==0.19.0
slowapi==0.1.9

# Whisper transcription (Hugging Face Transformers)
transformers==4.57.1  # Versión específica requerida por KaniTTS
datasets[audio]>=2.14.0
accelerate>=0.20.0
torch>=2.0.0
torchaudio>=2.0.0

# Text-to-Speech (KaniTTS)
# NOTA: kani-tts requiere nemo-toolkit[all] que es muy pesado
# Instalar manualmente con: pip install kani-tts --no-deps && pip install transformers==4.57.1 soundfile
# O instalar todas las dependencias: pip install kani-tts
kani-tts>=0.1.0
soundfile>=0.12.0  # Requerido por KaniTTS para guardar audio
typing_extensions>=4.0.0  # Requerido por dependencias de kani-tts

